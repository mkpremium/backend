{
  "devDependencies": null,
  "scripts": {
    "migrate": "node --require source-map-support/register --require dotenv/config migrations/seed_all.js",
    "build_index": "node --require source-map-support/register --require dotenv/config indexes.js",
    "metadata": "node --require source-map-support/register --require dotenv/config migrations/seed_metadata.js",
    "family": "node --require source-map-support/register --require dotenv/config migrations/seed_family.js",
    "invalidate": "node --require source-map-support/register --require dotenv/config migrations/seed_invalidate.js",
    "denormalize": "node --require source-map-support/register --require dotenv/config migrations/seed_denormalize.js"
  },
  "betterScripts": null
}
