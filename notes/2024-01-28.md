## Trigger import

1. Reset postgres
```sql
truncate table building cascade;
truncate table contact cascade;
truncate table "user" cascade;
truncate table "domain_event" cascade;
UPDATE couchbase_document SET "migratedAt" = NULL where "migratedAt" IS NOT NULL
```
2. Run worker con `TRIGGER_OPERATORS_MIGRATION` y `TRIGGER_WORKSHEET_QUEUES_MIGRATION` = true.
3. Flipper asignado a Tania no se encontro (88959824-04b7-41b1-b70e-2b38bdc77891)
4. Trigger building import
